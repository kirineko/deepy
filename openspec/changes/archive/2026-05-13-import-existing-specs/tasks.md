# Tasks

- [x] Inventory existing `spec/*.md` documents.
- [x] Treat existing spec content as completed baseline, not active changes.
- [x] Create OpenSpec domain specs under `openspec/specs/`.
- [x] Preserve original source documents under the archive directory.
- [x] Ensure `openspec/` and `.codex/` are ignored by Git.
