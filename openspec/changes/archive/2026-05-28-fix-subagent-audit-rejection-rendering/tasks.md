## 1. Regression Coverage

- [x] 1.1 Add a terminal UI regression test for successful structured subagent output whose report mentions an audit approval rejection.
- [x] 1.2 Keep coverage that raw SDK audit rejection output still renders as rejected.

## 2. Rendering Fix

- [x] 2.1 Update stable terminal stream rendering so audit-rejection override is not applied to explicit successful structured tool outputs.
- [x] 2.2 Run focused terminal UI tests and strict OpenSpec validation.
