## Implementation

- [x] Update the `Update` approval view so metadata contains only the edit count.
- [x] Add regression coverage proving the approval view omits `summary` and raw
      old/new content.
- [x] Validate the OpenSpec change and run focused tests.
